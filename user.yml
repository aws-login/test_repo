- name: variable example
  hosts: all
  become: yes
  gather_facts: false
  vars:
          user: parker
  tasks:
        - name: adduser {{ user }}
          user:
              name: "{{ user }}"
              state: present
